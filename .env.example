# Database Configuration
# Vercel Postgres connection string (automatically provided when you create a Vercel Postgres database)
POSTGRES_URL=postgresql://username:password@host:5432/database

# NextAuth Configuration
# Generate a random secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-here-generate-with-openssl

# NextAuth URL (use your deployment URL in production)
# For local development: http://localhost:3000
# For production: https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
